"""Target-neutral schemas and templates bundled with the distribution.

Source-checkout resources have canonical counterparts at repository level;
these packaged copies are the wheel fallback. Resource-parity tests keep the
two locations byte-identical so installed and checkout executions enforce the
same contracts.
"""
